## Findings

- [x] README.md:16 — [low] README claims the publishable package is at a `0.0.0` alpha snapshot while root `package.json` declares `1.0.0` — release/status documentation can mislead users and npm consumers about package maturity — align the README status with the published package version or reset the root package version before release (FIXED: README status now states the root `pdf-2-llm` package declares version `1.0.0` and is configured for public npm publishing — verified by `rg -n '0\\.0\\.0 alpha-readiness snapshot|version .*1\\.0\\.0|"version": "1\\.0\\.0"' README.md package.json` and `npm run lint`)
- [x] scripts/qa/run-corpus.mjs:161 — [medium] corpus acceptance loading ignores `warnings.allowed`, `metrics.maxUnexpectedWarnings`, snippets, and must/mustNot criteria — reviewed acceptance files can pass corpus runs even when outputs contain unexpected warnings or miss required snippets/behaviors — parse and enforce the remaining acceptance fields, or fail when unsupported acceptance fields are present without a dedicated checker (FIXED: corpus runner now parses snippets, warning allowlists/budgets, and behavior criteria, enforces snippets and unexpected warning limits, and rejects unknown criteria without runner support — verified by `node --test packages/pdf2md/test/run-corpus-acceptance.test.mjs`, `npm run corpus:run:text`, `node scripts/qa/run-corpus.mjs --id synthetic-vector-figure --assert-markdown`, and `npm run lint`)
- [x] corpus/manifest.json:253 — [medium] 16 manifest entries are missing the `corpus/baselines/<id>/analysis.json` static-analysis artifact required by the corpus policy — release gates can be promoted without the recorded analysis that reviewers rely on for provenance and PDF feature coverage — generate and commit missing analysis files or mark those entries non-gating until analysis artifacts exist (FIXED: generated the 16 missing static-analysis artifacts and their referenced tool-output JSON files, then updated the corpus inventory to cover all 34 manifest entries — verified by `node scripts/corpus/analyze.mjs --id ...` for the 16 missing IDs, `npm run corpus:analyze`, `npm run corpus:validate`, `comm -23 <(jq -r '.entries[].id' corpus/manifest.json | sort) <(find corpus/baselines -mindepth 2 -maxdepth 2 -name analysis.json -printf '%h\n' | xargs -n1 basename | sort)`, and matching `34` analysis files to `34` manifest entries)
- [x] packages/pdf2md/src/webgpu-provider.mjs:45 — [medium] WebGPU OCR planning does not skip a single page whose `pixelCount` exceeds `maxBatchPixels` — callers can configure a maximum batch size yet still receive an oversized one-page batch that violates the advertised resource limit — mark pages over `maxBatchPixels` as skipped before batching and cover the case with a provider test (FIXED: pages with `pixelCount > maxBatchPixels` now get `exceeds-batch-pixel-limit` status before batching, with a regression test for an oversized single page — verified by `node --test packages/pdf2md/test/webgpu-provider.test.mjs` and `npm run lint`)
- [x] packages/pdf2md/src/pdf-parser.mjs:430 — [medium] tolerant object-scan repair parses discovered objects without forwarding the caller's `maxDepth` — malformed PDFs can bypass a configured depth limit during repair even though the normal xref path enforces it — pass `maxDepth` into repair-path `parseIndirectObjectAt` calls and add a tolerant-mode regression fixture (FIXED: object-scan repair now forwards `maxDepth` into `parseIndirectObjectAt` and treats depth-limit violations as fatal repair errors, with a damaged-xref regression test — verified by `node --test packages/pdf2md/test/parser.test.mjs` and `npm run lint`)
- [x] packages/pdf2md/src/figure-detection.mjs:150 — [low] figure Markdown insertion locates captions with a document-wide `indexOf` — duplicate caption text can attach multiple figure assets to the first matching caption and shift source maps incorrectly — resolve insertion points from page/source-map regions or continue searches after the prior matched caption (FIXED: figure insertion now selects unused caption ranges that overlap the figure page's source-map regions, preventing duplicate captions from reusing the first match — verified by `node --test packages/pdf2md/test/figure-detection.test.mjs`, `node --test packages/pdf2md/test/convert.test.mjs --test-name-pattern=figure`, and `npm run lint`)
- [x] scripts/corpus/validate-acceptance.mjs:349 — [medium] acceptance validation never requires or range-checks `metrics.minTextCoverage` — a gating acceptance file can omit the corpus' baseline coverage threshold while still passing validation — require `minTextCoverage` for gating files and validate it as a 0-to-1 metric (FIXED: gating acceptance files now require `metrics.minTextCoverage`, and the metric is validated as a 0-to-1 number when present, with CLI regression coverage — verified by `node --test packages/pdf2md/test/validate-acceptance.test.mjs`, `npm run corpus:acceptance`, and `npm run lint`)

## Findings

- [x] packages/pdf2md/test/convert.test.mjs:1463 — [medium] figure and low-confidence equation tests approve Markdown image links whose `figure-preview` or `equation-preview` assets contain only a logical PNG path with no content or render handle — consumers cannot write the promised sidecar from `ConvertResult`, so the returned Markdown renders as broken images even when every API test passes — return actual preview bytes/base64 (and test their decodability), or omit the image link and emit an explicit metadata-only fallback until raster generation exists (FIXED: replaced unwritable preview links/assets with explicit metadata-only Markdown, warning, IR, and diagnostic fallbacks while retaining figure/equation source metadata — verified by `node --test packages/pdf2md/test/text-extract.test.mjs packages/pdf2md/test/figure-detection.test.mjs packages/pdf2md/test/convert.test.mjs`, `npm run lint`, and `npm run test:api`)

- [x] packages/pdf2md/test/acceptance-review-workflow.test.mjs:16 — [medium] the passing review-workflow test uses a zero-byte file as its rendered PNG evidence — the release gate explicitly codifies that an empty or corrupt preview is sufficient for human-review provenance, so unusable previews can keep passing after the current corpus fixture is repaired — generate a minimally valid image in the test and make the audit decode or otherwise validate every declared preview before accepting the evidence (FIXED: the review audit now validates every indexed preview path and bounded image, including PNG CRCs and decoded scanlines, while the passing fixture uses a valid 1x1 PNG and an empty-image regression fails explicitly — verified by `node --test packages/pdf2md/test/acceptance-review-workflow.test.mjs`, `npm run corpus:review-audit`, `npm run lint`, and `npm run test:api`)

- [x] corpus/reports/performance-regression.json:12 — [medium] every current benchmark input is the same path as its baseline input, making all 11 regression checks self-comparisons — the committed report always passes unchanged values and provides no evidence that current performance was tested against a prior baseline — generate fresh current reports under `.temp/qa`, compare those to the checked-in baselines, and only then write the regression summary (DEFERRED: the report workflow now generates fresh `.temp/qa` inputs and rejects every same-path current/baseline pair, but two fresh long-memory runs consistently failed the existing 16 MiB heap gate at about 18.7 MiB, so the committed summary was correctly not rewritten; the newly recorded IR-memory finding tracks that prerequisite)
- [x] packages/pdf2md/src/text-extract.mjs:1811 — [medium] the populated document IR expands 5,779 extracted NIST text lines into 56,622 separate span objects and an 8.6 MiB serialized `ir`, causing fresh `qa:benchmark:long-memory` runs to retain about 18.7 MiB of heap against the accepted 16 MiB limit — coalesce adjacent compatible source spans without losing text, geometry, style, direction, confidence, or source fidelity, then regenerate the real performance-regression report from fresh `.temp/qa` inputs (FIXED: coalesced only attribute-compatible, geometrically adjacent IR spans while preserving text, glyph IDs, and union bounds, reducing the NIST case to 6,807 spans and 1.5 MiB IR; retained heap fell to 10.8 MiB and the regenerated distinct-input report passed all 11 checks — verified by `node --test packages/pdf2md/test/text-extract.test.mjs packages/pdf2md/test/convert.test.mjs packages/pdf2md/test/ir-snapshot.test.mjs`, `npm run qa:benchmark:long-memory`, `npm run qa:performance-regression:report`, `npm run lint`, and `npm run test:api`)
- [x] corpus/reports/package-size-budget.json:9 — [medium] the committed size report measures the internal `@pdf-2-llm/pdf2md@0.0.0` workspace instead of the public `pdf-2-llm@1.2.1` package and predates the current forbidden-path checks, while the root dry-run tarball has different size and entry counts — release evidence can pass while omitting files, growth, or forbidden paths on the actual published package — regenerate through the current root-package checker and keep the report command pinned to the public package identity (FIXED: package-size commands now assert the `pdf-2-llm` identity and the regenerated root tarball report records `pdf-2-llm@1.2.1`, 117,093 packed bytes, 514,331 unpacked bytes, 38 entries, and no forbidden files — verified by `node --test packages/pdf2md/test/benchmark.test.mjs`, `npm run qa:package-size:report`, `npm run lint`, and `npm run test:api`)
- [x] corpus/baselines/synthetic-vertical-writing/previews/index.json:15 — [medium] the accepted CJK, RTL, and vertical-writing fixtures map Unicode through a non-embedded synthetic Type 1 font, so their previews omit or misrender the target text and the vertical page is completely blank — gating cases validate extraction of invisible text while still satisfying the rendered-preview human-review audit — generate all three fixtures with embedded renderable fonts or visible vector glyphs, then refresh their manifest hashes, oracles, previews, and reviewed acceptance evidence (FIXED: the generator now pairs its ToUnicode extraction layer with deterministic, licensed vector glyph paths; refreshed PDFs, hashes, analysis, oracles, visibly reviewed 72-DPI previews, and 2026-07-10 acceptance evidence cover Hebrew, horizontal Japanese, and both vertical columns — verified by `npm run corpus:generate`, targeted `scripts/corpus/analyze.mjs` and `extract-oracles.mjs` runs, `npm run corpus:validate`, `npm run corpus:acceptance`, `npm run corpus:run:multilingual`, `npm run corpus:review-audit`, and `npm run lint`)

- [ ] packages/pdf2md/examples/browser-basic.html:331 — [medium] The published browser example's Load Fixture URL points outside the package allowlist to `corpus/generated/synthetic-simple-text.pdf` — installed-package consumers always receive a missing fixture instead of the advertised one-click conversion — ship the fixture under `examples/`, update the URL, or remove/disable the fixture action in the published example
- [ ] packages/pdf2md/src/webgpu-preprocess.mjs:440 — [low] `preprocessing.bias: 0` is silently normalized back to the default value `7` — callers cannot request an unbiased adaptive threshold even though the public option and docs accept a numeric bias — normalize zero explicitly (and define/clamp the supported signed range consistently for CPU, WGSL parameters, and declarations) and add a non-border regression test
- [x] packages/pdf2md/src/stream-filters.mjs:76 — [high] `FlateDecode` inflates the entire stream before enforcing `maxDecodedStreamBytes` — a small compressed PDF stream can allocate an arbitrarily large buffer and exhaust the process before the hostile-input limit is checked — pass the remaining byte cap into a bounded inflater (for Node, `inflateSync(..., { maxOutputLength })`), translate limit errors to `pdf.stream.decoded_too_large`, and add a high-ratio deflate-bomb regression (FIXED: Flate inflation now uses the configured output bound and maps native limit failures to the stable decoded-too-large error — verified by `node --test packages/pdf2md/test/stream-filters.test.mjs`, `npm run lint`, and `npm run test:api`)
- [x] packages/pdf2md/src/font-encoding.mjs:89 — [high] `beginbfrange` mappings are expanded from `startCode` through `endCode` with no range or total-entry limit — a tiny untrusted ToUnicode CMap can declare billions of codes and exhaust CPU/memory despite decoded-stream limits — cap per-range and aggregate CMap mappings under an explicit parser security limit, reject or truncate oversized ranges with a stable warning/error code, and fuzz the boundary (FIXED: added the configurable `maxCMapMappings` budget with immediate per-range and aggregate rejection under `pdf.cmap_mapping_limit_exceeded`, plus boundary unit, malicious-PDF, and fuzz coverage — verified by `npm run lint`, `npm run test:api`, and `npm run fuzz:smoke`)
- [ ] packages/pdf2md/src/font-encoding.mjs:53 — [medium] fonts declared as WinAnsi, Standard, or MacRoman fall back to raw Latin-1 decoding even though those encodings have different byte-to-Unicode maps and are marked trusted — common punctuation, symbols, and non-ASCII text are silently corrupted without a unicode-mapping warning — implement the three encoding tables (including Encoding `Differences` where supported) and add byte-level regression fixtures such as WinAnsi `0x80` mapping to U+20AC
- [ ] packages/pdf2md/src/content-stream.mjs:135 — [medium] the tokenizer has no inline-image (`BI ... ID ... EI`) state and parses image data bytes as ordinary operands/operators — inline scans are absent from image/scan diagnostics and coincidental operator bytes can emit phantom text or mutate extraction state — parse inline-image dictionaries and skip bounded binary payloads through a valid `EI` delimiter, then expose their geometry to image-draw detection and add adversarial payload tests
- [ ] packages/pdf2md/src/content-stream.mjs:1131 — [medium] glyph and line geometry always advances `cursor.x` and keeps `y` fixed even when the effective text matrix is rotated or vertical — vertical text is tagged with `direction: "vertical"` but receives horizontal bounds, corrupting layout ordering, overlap checks, and source-map regions — transform each glyph advance/bounding box through the effective text matrix and add assertions for coordinates and extents under 90-degree and skewed matrices
- [ ] packages/pdf2md/src/content-stream.mjs:1204 — [medium] `/Widths` lookup is indexed by each decoded Unicode code point instead of the original PDF character code/CID — ToUnicode remaps and multi-codepoint mappings receive incorrect advances, which then drive spacing inference, line bounds, layout, and source maps — preserve source glyph codes during CMap decoding and measure each glyph from its PDF code before associating the mapped Unicode text
- [ ] packages/pdf2md/src/content-stream.mjs:1293 — [medium] nested content-stream arrays/dictionaries recurse without any depth or token budget — a compact untrusted stream can overflow the JavaScript call stack and bypass the configured `security.maxDepth` parser behavior — thread an explicit depth/token budget through `readValue`, `readArray`, and `readDictionary`, fail with a stable bounded-input error, and add deeply nested content-stream tests
- [ ] packages/pdf2md/src/pdf-parser.mjs:162 — [medium] the Standard RC4 encryption context decrypts streams only and never decrypts literal or hex strings in indirect objects — a correct password can recover page content while leaving encrypted outlines, annotations, form values, metadata, and other public diagnostics as ciphertext — decrypt all strings recursively with each containing object's object/generation key (excluding the encryption dictionary), and add an encrypted fixture that asserts non-stream strings
- [ ] packages/pdf2md/src/pdf-parser.mjs:1693 — [medium] XObject resources expose only summary metadata and content-stream `Do` execution ignores `/Subtype /Form` streams — PDFs that wrap page text or nested images in reusable Form XObjects produce blank or incomplete Markdown, source maps, and scan diagnostics — retain Form stream/resources/matrix data and recursively interpret `Do` with composed transforms plus cycle/depth limits, backed by nested-Form fixtures
- [x] packages/pdf2md/src/pdf-parser.mjs:88 — [high] `maxDecodedStreamBytes` is enforced per stream but there is no document-wide decoded-byte budget, and every stream retains decoded bytes plus a full Latin-1 text copy — many individually sub-limit compressed streams can drive unbounded aggregate memory and terminate the process without a security warning — track cumulative decoded/retained bytes under an explicit total limit, avoid eager text copies for binary streams, and add a many-stream expansion regression (FIXED: added `maxTotalDecodedStreamBytes` with object-keyed cumulative accounting and bounded remaining-cap decoding, exposed retained-byte diagnostics, and made stream text lazy with many-stream and zero-boundary regressions — verified by `npm run test:api`, `npm run fuzz:smoke`, and `npm run build`)
- [ ] packages/pdf2md/src/pdf-parser.mjs:1136 — [medium] indirect stream `/Length` references recurse until a cycle but never enforce `security.maxDepth` — a long acyclic reference chain can overflow the stack (and repeatedly linearly scan xref entries) despite a low configured depth limit — resolve the chain iteratively with an indexed xref lookup, cap hops at `maxDepth`, and test the exact boundary and cycle cases
- [ ] packages/pdf2md/src/pdf-parser.mjs:1946 — [low] a backslash followed by CR, LF, or CRLF in an object literal string is retained as newline content instead of being removed as PDF line continuation — outlines, form values, annotations, and tagged text can gain spurious line breaks, with CRLF handled inconsistently — consume escaped line endings during literal-string parsing and add CR, LF, and CRLF regression cases
- [ ] packages/pdf2md/src/document-interactions.mjs:164 — [medium] AcroForm field-tree and EmbeddedFiles name-tree traversal is recursively cycle-safe but has no depth budget — long acyclic trees can overflow the call stack after parsing despite `security.maxDepth` — pass the configured limit into interaction extraction (or use iterative traversal), enforce it consistently for both trees, and add boundary fixtures
- [ ] packages/pdf2md/src/document-interactions.mjs:223 — [medium] form geometry is read only from the field dictionary even when a terminal field has separate `/Subtype /Widget` children that own `/Rect` — common AcroForm fields receive a page index but lose coordinates in diagnostics and document IR — select the relevant widget child per page and inherit its rectangle/appearance state, with a split field-widget regression fixture
- [ ] packages/pdf2md/src/document-interactions.mjs:607 — [low] attachment asset paths use only the sanitized filename, allow `.`/`..` as basenames, and do not deduplicate collisions — materializing returned assets can resolve outside `assets/attachments` or overwrite a same-named attachment — reject dot segments and generate deterministic unique paths (for example with the attachment index or collision suffix), then test duplicate and hostile filenames
- [ ] packages/pdf2md/src/figure-detection.mjs:205 — [medium] source-map entries that start before but end after a figure insertion are left unchanged — inserting an image inside a mapped caption/block makes that entry's `markdownEnd` stale and truncates its public source range — shift both offsets for entries after the insertion and extend only `markdownEnd` for spanning entries, with a regression where the caption starts inside a mapped block
- [x] packages/pdf2md/src/text-extract.mjs:976 — [high] source-derived LTR Markdown text does not escape HTML-significant `<`, `>`, or `&` characters — an untrusted PDF can inject raw tags or scripts that execute in downstream Markdown renderers with HTML enabled — HTML-escape source text before Markdown escaping while preserving only converter-generated HTML/autolinks, and add paragraph, heading, list, and table XSS regressions (FIXED: source LTR text now entity-escapes HTML-significant characters before Markdown escaping, tag-shaped prose bypasses heuristic equation rendering, and converter-owned autolinks remain intact — verified by `node --test packages/pdf2md/test/text-extract.test.mjs`, `npm run lint`, and `npm run test:api`)
- [x] packages/pdf2md/src/text-extract.mjs:463 — [high] code fencing only toggles between exactly three backticks and three tildes — source containing both fence forms (or a longer matching run) can close the generated block and expose subsequent PDF text as executable raw HTML — choose a fence character and length strictly longer than every run in the block, and add mixed-fence plus HTML-payload regressions (FIXED: code blocks now choose the shorter safe backtick-or-tilde delimiter whose length exceeds every matching source run, keeping mixed fences and raw HTML inside the block — verified by `node --test packages/pdf2md/test/text-extract.test.mjs`, `npm run lint`, and `npm run test:api`)
- [ ] packages/pdf2md/src/text-extract.mjs:54 — [medium] each stream in a page `/Contents` array is interpreted with a new graphics and text state — PDF content arrays form one ordered sequence, so state split across streams (for example `BT`/`Tf` followed by `Tj`) is lost and valid text, transforms, or paths disappear — concatenate streams with a safe separator or carry interpreter state across them, then test text and `q`/`Q` spanning stream boundaries
- [ ] packages/pdf2md/src/text-extract.mjs:257 — [medium] a list continuing onto a new page with `markdown.pageAnchors` enabled appends the item to the newly inserted page-anchor block because `previousWasList` remains true — the anchor HTML is corrupted and the list item is mislabeled/mapped as `page_anchor` — reset list aggregation at page boundaries or append only to a verified preceding list block, with a multi-page anchored-list regression
- [x] packages/pdf2md/src/index.mjs:326 — [high] document IR page elements are populated from OCR lines but never from selected PDF text or detected tables — ordinary digital PDFs return non-empty Markdown with empty/incomplete `ir.pages[*].elements`, breaking the advertised structured-data contract — build page text elements from reconciled PDF/OCR lines and table elements from emitted tables without duplication, then replace the empty digital IR snapshot with content assertions (FIXED: reconciled Markdown blocks now project selected PDF/OCR lines and emitted tables into page IR, including span-aware rows and CSV asset references without duplicating table-cell text, and the reviewed digital snapshot now asserts content — verified by `npm run lint` and `npm run test:api`)
- [ ] packages/pdf2md/src/index.mjs:185 — [medium] `tables.enabled: false` only disables CSV assets while ruled/borderless table detection and Markdown conversion still run, and `tables.htmlFallback` is never consulted — callers cannot disable table heuristics or the declared HTML fallback behavior — gate detection/serialization with `tables.enabled`, define and honor the no-HTML span fallback, and add option-level output tests
- [ ] packages/pdf2md/src/index.mjs:623 — [medium] default CSV sidecars do not neutralize cells beginning with spreadsheet formula markers (`=`, `+`, `-`, `@`) — opening a sidecar generated from an untrusted PDF can execute attacker-controlled formulas in common spreadsheet applications — apply a documented formula-injection-safe encoding/prefix (or an explicit raw opt-in) before RFC 4180 quoting and add adversarial cell tests
- [ ] packages/pdf2md/src/index.mjs:143 — [medium] `security.maxPages` is enforced only after `parsePdfDocument` has fully walked and materialized the page tree — an oversized untrusted document consumes the CPU/memory the configured limit is supposed to bound before being discarded — pass the page budget into page-tree traversal, stop as soon as it is exceeded with the stable page-limit code, and test early termination
- [ ] packages/pdf2md/src/index.mjs:1212 — [medium] `validateSecurityLimits` omits `maxBytes`, so `NaN` silently makes both input-size comparisons false and disables the primary untrusted-input cap — malformed security configuration bypasses the documented non-negative-integer invariant instead of failing closed — validate every security field centrally (including `maxBytes`, `maxObjects`, and `maxImagePixels`) before reading input and add NaN/infinity/fraction regressions
- [ ] packages/pdf2md/src/cli.mjs:166 — [low] `--output` writes directly without creating its parent directory — documented clean-checkout commands such as `--output .temp/simple.md` fail with `ENOENT` unless the user pre-creates `.temp`, unlike `--debug-trace` — create `dirname(resolve(outputPath))` recursively before writing and add a nested-output CLI test
- [ ] packages/pdf2md/src/cli.mjs:63 — [low] unknown options and extra positional arguments are silently ignored — a typo such as `--jsoon` exits successfully with the wrong output format, making CLI automation appear valid while producing unintended data — reject unrecognized/duplicate arguments, support `--` for dash-prefixed paths, and add parser/exit-code tests
- [x] packages/pdf2md/src/runtime.mjs:37 — [high] `FlateDecode` is implemented only through Node's synchronous `zlib` builtin — ordinary compressed PDFs fail parsing in the advertised browser and worker entrypoints, leaving only the raw uncompressed fallback and usually empty Markdown — provide a bounded cross-runtime inflater (or make the parser pipeline async around `DecompressionStream`/a packaged codec) and run compressed-PDF browser/worker acceptance tests (FIXED: browser and worker runtimes now use chunked `fflate` decompression with the same output cap and Adler-32 validation while Node retains bounded native zlib, with compressed-entrypoint and portable deflate-bomb regressions plus a declared audited runtime dependency — verified by `node --test packages/pdf2md/test/stream-filters.test.mjs packages/pdf2md/test/convert.test.mjs`, `npm run lint`, `npm run test:api`, `npm run build`, `npm run qa:dependencies`, `npm pack --dry-run --json`, and an installed-tarball browser-entrypoint smoke test)
- [ ] packages/pdf2md/src/text-extract.mjs:56 — [medium] page content extraction does not apply `/UserUnit`, CropBox origin, or page `/Rotate` while page dimensions do apply UserUnit — text/image/ruling coordinates can disagree with page bounds, corrupting scan coverage, table/figure geometry, reading order, and source maps on valid non-default pages — compose a normalized page transform into each content-stream interpreter and add cropped, scaled, and rotated coordinate assertions
- [ ] packages/pdf2md/src/index.mjs:481 — [medium] equation and figure "image" assets contain only an expected path with no content, while generated Markdown links to that nonexistent file and no API/CLI writer creates it — visual content described as preserved is actually unavailable to consumers and rendered Markdown has broken images — generate bounded preview bytes through the raster path (or stop emitting image links/claims until available) and add asset-link materialization tests
- [ ] packages/pdf2md/src/pdf-parser.mjs:700 — [medium] normal classic/xref-stream parsing materializes all declared entries before the top-level `maxObjects` check — a document far above a low configured object budget still consumes avoidable parse time and memory, while repair mode correctly stops incrementally — enforce the budget while decoding each section and while merging `Prev` sections, with early-failure tests for both xref formats
- [ ] packages/pdf2md/src/pdf-parser.mjs:1288 — [medium] non-BOM PDF text strings are decoded as Latin-1 instead of PDFDocEncoding — outlines, structure attributes, form values, annotations, attachment names, and signature metadata silently corrupt characters whose PDFDocEncoding mapping differs — implement the PDFDocEncoding table and add byte-level interaction/outline fixtures alongside UTF-16 coverage
- [ ] packages/pdf2md/src/font-encoding.mjs:180 — [medium] a CMap destination is converted with `String.fromCharCode(...codeUnits)` in one spread call — one large but stream-limit-compliant `bfchar` value can exceed the engine argument limit and throw an unstructured `RangeError` — cap mapping destination size and decode UTF-16BE incrementally/chunked with a stable malformed-CMap error plus boundary fuzz coverage
- [x] packages/pdf2md/src/content-stream.mjs:17 — [high] content streams are fully tokenized and interpreted without operator, output, or graphics/marked-content stack budgets — a byte-limit-compliant stream of repeated `q`, `BMC`, text, or path operators can expand into millions of cloned states/records and exhaust memory — add explicit operator/output/depth limits tied to security options, stream tokens incrementally where practical, and fuzz repeated-state/operator boundaries (FIXED: content streams are now tokenized incrementally and enforce document-wide per-channel operation/output budgets plus `maxDepth` on graphics and marked-content stacks, returning structured limit diagnostics before text glyph, path, or image expansion — verified by `node --test packages/pdf2md/test/content-stream.test.mjs packages/pdf2md/test/security-limits.test.mjs`, `npm run lint`, `npm run test:api`, `npm run fuzz:smoke`, `npm run qa:malformed`, and `npm run build`)
- [ ] packages/pdf2md/src/index.mjs:311 — [low] any input with a valid `%PDF-` header but no parsed pages is labeled `ir.sourceType: "digital"` — malformed or unsupported documents can be routed downstream as digital despite having no extraction evidence — use `unknown` when parsing/page classification is unavailable (or derive from actual fallback signals) and add a header-only parse-failure regression
- [ ] README.md:16 — [low] the current-status section says the root package declares version 1.1.0 while package.json declares 1.2.1 — users and release reviewers receive stale package-version guidance — derive the displayed version during release preparation or update the README alongside every version bump
- [ ] docs/warnings-confidence.md:424 — [low] the focused validation command references nonexistent root-level test/schema.test.mjs, test/convert.test.mjs, and test/parser.test.mjs paths — maintainers following the warning-contract documentation get an immediate file-not-found failure instead of validation — prefix the paths with packages/pdf2md/ or invoke the workspace test script with an appropriate filter
- [ ] docs/pdf-to-markdown-implementation-plan.md:570 — [medium] the tracked plan marks configurable page rendering and thumbnail rendering complete even though the architecture and API state that raster support is metadata-only and retains no rendered buffers — checked status overstates a core capability and makes the plan unreliable for release and agent coordination — reopen unimplemented raster/OCR/WASM milestones or explicitly split contract/planning completion from executable feature completion with evidence links
- [ ] scripts/corpus/validate-acceptance.mjs:255 — [medium] acceptance validation checks required fields but accepts unknown or misspelled top-level and nested keys, orphan YAML files with no manifest entry, and warning codes outside the public warning taxonomy — malformed criteria can pass CI while their intended gates are silently ignored — parse YAML structurally, validate it against a closed schema, require every non-template file to map to exactly one manifest entry, and validate warning codes against the exported constants
- [ ] scripts/build/build-threaded-wasm.mjs:51 — [medium] a failed optional threaded-WASM rebuild reports unavailable but does not remove a previously produced packages/pdf2md/src/wasm/pdf2md_core.threaded.wasm — later package builds can silently ship a stale binary that no longer matches the Rust source or reported build state — remove the destination before building or delete it on every failure path and cover the stale-artifact case with a script test
- [ ] scripts/corpus/retrieve.mjs:366 — [medium] remote corpus downloads are buffered in full with response.arrayBuffer() without a byte limit or bounded streaming — a compromised or unexpectedly large candidate URL can exhaust developer or CI memory before the PDF reaches manifest and parser limits — enforce a configured maximum from Content-Length when present and stream the body to a temporary file while aborting once the cap is exceeded
- [ ] scripts/corpus/generate-fixtures.mjs:563 — [medium] generated acceptance files are always written as gating true and stamped humanReviewedBy "codex" with a fixed review date — regeneration can overwrite review metadata and causes unreviewed fixtures to satisfy the review-before-gating audit automatically — generate new criteria as non-gating with empty review fields or preserve separately approved acceptance metadata until an explicit human review step promotes the fixture
- [x] scripts/qa/run-corpus.mjs:640 — [high] recognized must and mustNot criteria are counted as satisfied solely by membership in name whitelists, while structure.expected, assets.required, minTextCoverage, reading-order, character-error, rendered-HTML, and other acceptance fields are not executed by the runner — gating corpus cases can pass even when their primary behavioral requirements are false, creating misleading release confidence — replace the whitelists with criterion-to-predicate implementations, parse and enforce every supported acceptance field, and fail closed whenever a gating criterion has no executable checker (FIXED: replaced name whitelists with executable behavior/structure predicate registries and made the runner enforce source/mode, reviewed text and order metrics, rendered HTML, tagged/running-content, memory, forms, assets, snippets, warnings, and review provenance while registry-auditing every committed label and failing closed on unknowns — verified by `node --test packages/pdf2md/test/run-corpus-acceptance.test.mjs`, `npm run lint`, `npm run test:api`, `npm run corpus:acceptance`, `npm run corpus:run:text`, `npm run corpus:run:multilingual`, and `node scripts/qa/run-corpus.mjs --gate ocr-v1 --assert-markdown`)
- [ ] scripts/qa/run-corpus.mjs:703 — [medium] page-scoped snippet checks return true whenever the source map has no entries — a snippet on the wrong page or with no provenance can satisfy a page-specific acceptance assertion — fail the page assertion when source-map evidence is absent, or require an explicit document-wide snippet mode for checks that do not need page provenance
- [ ] scripts/qa/check-webgpu-comparison.mjs:28 — [medium] an empty benchmark comparisons array passes the WebGPU parity gate whenever --require-speedup is not set — a failed or malformed benchmark that produced no evidence can be reported as successful QA — require at least one comparison and validate the report schema and numeric threshold before evaluating parity or speedup
- [ ] scripts/qa/check-table-detection.mjs:42 — [medium] table true positives are computed only from the lesser of expected and actual table counts without matching content, format, or source region — an unrelated table can replace the expected table and still produce perfect precision and recall — match expected and actual tables by normalized cells and format or by source-map region before counting true positives
- [x] scripts/qa/benchmark.mjs:119 — [high] CPU/WebGPU accepted-output equivalence compares only Markdown length, text-line count, and warning-code arrays — different Markdown with equal length and changed source maps, assets, IR, diagnostics, or confidence can pass the acceleration parity gate — compare canonicalized full public result contracts or stable hashes of every correctness-bearing field while excluding only timing and provider-specific diagnostics (FIXED: parity now compares canonical SHA-256 hashes of the complete public result contract, excluding only elapsed timing and WebGPU request/provider diagnostics — verified by node --test packages/pdf2md/test/benchmark.test.mjs, npm run qa:webgpu-comparison, npm run lint, and npm run test:api)
- [ ] scripts/qa/browser-webgpu-preprocess.mjs:497 — [medium] Chrome shutdown sends SIGTERM and then waits without a deadline for the close event — a stuck browser can hang the benchmark indefinitely even after its own timeout has fired, blocking CI and leaving the local server open — race graceful shutdown against a short deadline, escalate to SIGKILL, and cover an unresponsive child process in a script-level test
- [x] corpus/accepted/nist-sp800-207-zero-trust.yaml:52 — [high] six NIST acceptance files whitelist `repeated_running_content`, `figure_asset_fallback`, `complex_table_html_fallback`, and `form_metadata_only`, but none is a declared or emitted public warning code — every NIST corpus case fails with 21 to 140 unexpected warning instances, breaking `corpus:run:all` and `npm run check` — replace the legacy names with reviewed current `warningCodes` values, set deliberate instance budgets, and validate every `warnings.allowed` entry against the public registry (FIXED: replaced all six legacy allowlists with their observed public warning codes, set zero unexpected-instance budgets, and made acceptance validation reject unknown codes and invalid budgets — verified by a focused six-document conversion warning audit, node --test packages/pdf2md/test/validate-acceptance.test.mjs, npm run corpus:validate, npm run corpus:acceptance, npm run lint, and npm run test:api)
- [ ] corpus/manifest.schema.json:84 — [low] the JSON Schema omits the validator's non-empty-string and source-type conditions, so URL entries without `url` and generated or mutated entries without `command` are schema-valid but rejected by `corpus:validate` — editor and external schema validation disagree with the repository's real manifest contract — add `minLength` constraints and `if`/`then` requirements that mirror `validate-manifest.mjs`, or generate both validators from one schema
- [ ] corpus/accepted/synthetic-header-footer.yaml:22 — [medium] the dedicated repeated-header/footer fixture defines no `runningContent` labels or precision/recall thresholds — `compare-running-content.mjs` explicitly skips this case, so the corpus gate can regress header/footer removal while the acceptance file still claims that behavior — add reviewed `expectedRemoved` and `expectedRetained` labels plus `minRunningContentPrecision` and `minRunningContentRecall`
- [ ] corpus/accepted/synthetic-damaged-xref.yaml:10 — [low] the damaged-xref acceptance contract requires `preserve_decrypted_text_order` even though this fixture is neither encrypted nor decrypted — the criterion documents and reports an unrelated behavior instead of the repair path's actual output invariant — replace it with the applicable text/order criterion and cover that criterion with an executable assertion
- [x] corpus/accepted/synthetic-complex-spanned-table.yaml:23 — [high] both snippet assertions mismatch the actual HTML-table Markdown: angle brackets are serialized as entities and the ad hoc acceptance reader preserves the backslashes in `\"net\"` — `npm run corpus:run:tables`, and therefore the broad `npm run check`, fails on this accepted fixture — assert the serialized Markdown strings (`&lt;`/`&gt;`) and use a scalar form that produces literal quotes, or replace the line parser with a real YAML parser (FIXED: aligned both snippets with the serialized HTML entities and used a single-quoted scalar without preserved escape backslashes — verified by npm run corpus:validate, npm run corpus:acceptance, and node scripts/qa/run-corpus.mjs --id synthetic-complex-spanned-table --assert-markdown)
- [x] corpus/accepted/plos-metagenomic-ml-meta-analysis-2016.yaml:2 — [high] assigning this public document to `tables-v1` makes `corpus:run:tables --assert-markdown` require `corpus/expected/plos-metagenomic-ml-meta-analysis-2016.md`, but that snapshot does not exist — the table release gate and broad `npm run check` fail with ENOENT once earlier synthetic cases pass — add and review the exact snapshot, or scope exact snapshot assertions to cases that deliberately provide one while retaining the public table-specific comparator (FIXED: split the table gate into exact snapshot checks for the five deterministic synthetic cases followed by acceptance-driven coverage of every tables-v1 case, so the public PLOS document no longer requires an undeclared full-document snapshot — verified by npm run corpus:run:tables:snapshots and node scripts/qa/run-corpus.mjs --id plos-metagenomic-ml-meta-analysis-2016 advancing past snapshot evaluation to the separately recorded source-type and control-character findings)
- [x] corpus/accepted/synthetic-two-column.yaml:27 — [high] this fixture and `synthetic-scientific-two-column` allow no warnings even though both deterministically emit the public `text.ordering_uncertain` warning for the column transition — `npm run corpus:run:layout`, and therefore `npm run check`, fails despite both Markdown snapshots matching — allow `text.ordering_uncertain` in both acceptance files or suppress the runtime warning only when resolved layout establishes trustworthy order (FIXED: allowlisted the registered ordering warning for the deterministic left-to-right column transition in both fixtures while retaining zero unexpected-warning budgets — verified by npm run corpus:validate, npm run corpus:acceptance, and npm run corpus:run:layout)
- [x] corpus/accepted/nist-sp800-213-iot-device.yaml:25 — [high] five NIST acceptance files contain title or legal-text snippets that current conversion either omits or maps to a different page (`800-213`, `800-218`, `800-30r1`, `800-61r2`, and `800-63b-4`) — individually running these accepted cases fails their snippet gates, so `corpus:run:all` and the broad check cannot pass — inspect the converter/source-map regressions against the reviewed PDFs and only then refresh page/text assertions where the acceptance data, rather than extraction, is stale (FIXED: repaired near-overlap intra-word spacing and same-baseline fragments across text objects, restoring the 63B legal/title text and the 61r2 systems heading, then narrowed only the stale cross-heading title literals and corrected the reviewed 63B legal-notice page to 3 — verified by a focused five-document source-map audit passing all 15 snippets, node --test packages/pdf2md/test/content-stream.test.mjs, npm run corpus:validate, npm run corpus:acceptance, npm run lint, and npm run test:api)
- [x] corpus/expected/nist-sp800-63b-4-public-table.md:1 — [high] current conversion fragments letters and shifts cell text throughout the reviewed authenticator-secrets table (`passcodes` becomes `passc odes`, `Activation secret` is split across cells) — `qa:public-tables` reports adjacency 0 and span accuracy 0.2 against this oracle, breaking the public-table release gate and broad check — repair scaled glyph spacing and table cell assignment while retaining this reviewed clean-text oracle as the regression target (FIXED: retained the reviewed oracle, assigned table spans from their leading edge with a font-relative boundary bias so trailing word fragments stay in their source cell, and glued overlapping hyphen continuations without a synthetic space — verified by npm run qa:public-tables at 22/22 adjacency and 15/15 span accuracy, focused content-stream and table-grid tests, npm run corpus:run:tables:snapshots, npm run qa:table-detection, npm run lint, and npm run test:api)
- [x] corpus/accepted/plos-metagenomic-ml-meta-analysis-2016.yaml:3 — [high] the PLOS acceptance declares document `sourceType: digital`, but scan detection deliberately classifies 24 pages as digital and two image-dominant, text-bearing figure pages as hybrid, producing document source type `hybrid` and failing the table gate — review the two page classifications and either accept the runtime hybrid contract or correct document-level source-type aggregation if figure-heavy pages should not reclassify a born-digital paper (FIXED: accepted and documented the intentional hybrid contract after reviewing image-dominant, text-bearing pages 8 and 11, while retaining pdf-text as the expected extraction mode — verified by a focused live conversion source-type audit, npm run corpus:validate, and npm run corpus:acceptance)
- [x] packages/pdf2md/src/font-encoding.mjs:69 — [high] raw Latin-1 fallback for the PLOS paper's unencoded F16 font emits 22 C0 control characters (including U+0015 and U+0001) into public Markdown, so the accepted `emit_binary_garbage` prohibition fails even though `text.unicode_mapping_suspect` is warned — recover the font's glyph mapping or replace unsafe unmapped controls with an explicit loss signal while preserving the warning, and add this real output as a regression (FIXED: render unmapped C0 and DEL bytes as reversible visible Unicode control pictures in both raw and partial-CMap fallback paths while retaining `text.unicode_mapping_suspect` — verified by focused font tests, the real PLOS regression at 0 hidden controls and 22 visible placeholders, a full PLOS corpus run passing all acceptance checks, npm run qa:malformed, npm run lint, and npm run test:api)

- [ ] packages/pdf2md/src/runtime.mjs:42 — [medium] the bounded Node Flate wrapper forwards `maxOutputLength: 0`, which zlib rejects as an invalid option before decoding — direct stream-filter callers using the supported non-negative zero-byte cap receive `pdf.stream.flate_failed` even for an otherwise valid compressed stream — use the smallest native bound at zero and retain an explicit decoded-length check, with empty and non-empty zero-cap regressions

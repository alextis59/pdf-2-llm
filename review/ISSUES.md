## Findings

- [ ] docs/release-notes.md:3 — [low] release notes still label the package snapshot as 0.0.0 while the root package and README declare 1.0.0 — release/readiness consumers get conflicting version guidance — align the heading and snapshot language with the publishable root package version or explicitly explain the workspace-internal 0.0.0 version
- [x] packages/pdf2md/src/cli.mjs:22 — [medium] CLI treats option values as positional inputs, so `pdf-2-llm --output out.md input.pdf` converts `out.md` instead of `input.pdf` — common flag ordering can fail or read the wrong file — parse argv by consuming flag values before selecting the positional input and validate missing option values (FIXED: added ordered CLI argv parsing that consumes `--output` values before selecting the input and rejects missing output paths — verified by `npm run test:api`)
- [ ] packages/pdf2md/src/wasm-loader.mjs:108 — [medium] default WASM loading uses `fetch` for the bundled `file:` URL, which fails in Node with `TypeError: fetch failed` — documented `await loadPdf2mdCoreWasm()` does not work for Node consumers unless they manually read bytes — handle `file:` URLs with Node fs/readFile or document and type the required explicit byte-source path
- [ ] packages/pdf2md/src/content-stream.mjs:689 — [medium] `TJ` numeric spacing operands are ignored when text is joined — PDFs that encode word gaps as positioning deltas can collapse words and produce incorrect glyph geometry — interpret numeric `TJ` adjustments against font size/scaling to advance positions and insert spaces when gaps exceed a word threshold
- [ ] scripts/qa/check-rendered-html.mjs:356 — [low] `--report` writes directly to the requested path without creating its parent directory — commands such as `--report .temp/qa/rendered-html.json` fail on a clean checkout even though other QA report commands create their output directory — call `mkdir(path.dirname(resolvedReportPath), { recursive: true })` before `writeFile`
- [ ] scripts/qa/compare-running-content.mjs:403 — [low] `--report` writes directly to the requested path without creating its parent directory — a clean-checkout report path under `.temp/qa` fails before producing the comparison summary — create the parent directory before writing the report
- [ ] scripts/qa/compare-tagged-structure.mjs:302 — [low] `--report` writes directly to the requested path without creating its parent directory — clean-checkout tagged-structure report commands fail when the target directory is absent — create the parent directory before writing the report

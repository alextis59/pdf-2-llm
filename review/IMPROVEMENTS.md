## Findings

- [x] docs/security-limits.md:300 — focused test commands point at `test/*.test.mjs` even though tests live under `packages/pdf2md/test` from the repo root — contributors following the docs get a module-not-found failure instead of the intended focused verification — update docs to use `node --test packages/pdf2md/test/...` paths or the workspace test script (FIXED: security-limits focused test command now uses repo-root `packages/pdf2md/test/...` paths — verified by `node --test packages/pdf2md/test/security-limits.test.mjs packages/pdf2md/test/malicious-pdf.test.mjs` and `npm run lint`)
- [x] scripts/qa/run-corpus.mjs:472 — `--update-snapshots` is accepted and the implementation plan marks corpus snapshot updating complete, but the runner only prints that updates are not implemented — maintainers can believe snapshot refresh support exists when the flag is a no-op — either implement snapshot writes or remove/document the flag as intentionally reserved (FIXED: reserved snapshot updates now fail explicitly before corpus execution and the plan no longer claims snapshot writes are complete — verified by `node --test packages/pdf2md/test/run-corpus-acceptance.test.mjs` and `npm run lint`)
- [ ] docs/wasm-loading.md:4 — specialty docs use `@pdf-2-llm/pdf2md/*` imports while README, API docs, release notes, and the root publishable package use `pdf-2-llm/*` — consumers can copy incompatible import specifiers depending on which doc they read — standardize docs on the intended public package name or clearly document dual-package publishing

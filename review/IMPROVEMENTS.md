## Findings

- [ ] packages/pdf2md/test/wasm-loader.test.mjs:104 — the threaded-load test passes the bundled single-thread binary as `threadedSource` and asserts only that diagnostics say `selected: "threaded"` — the suite can pass without ever instantiating the produced threaded artifact or proving its shared-memory imports work in a supported environment — add a build-backed test for `pdf2md_core.threaded.wasm` (or a minimal shared-memory fixture) and assert its module imports, shared memory, exports, and fallback behavior

- [ ] docs/pdf-to-markdown-webassembly-study.md:88 — the public API example imports the placeholder @your-scope/pdf2md package and presents an aspirational option shape without a current-API label — readers can copy an unresolvable package name or mistake study pseudocode for the shipped contract — use pdf-2-llm for the import and clearly label the snippet as a target API distinct from docs/api.md

- [ ] corpus/README.md:10 — the documented corpus layout omits the active `candidates.json` registry and `ocr/` injected-result fixtures — contributors cannot discover two inputs consumed by `retrieve.mjs`, the manifest, and the OCR corpus runner from the corpus entry guide — add both paths to the layout and briefly document their ownership and validation workflow
- [ ] corpus/expected/synthetic-cropped-page.md:1 — eleven reviewed Markdown snapshots for robust-parser, forms, and advanced fixtures are not read by any configured test or `npm run check` command — these expected artifacts can become stale while literal assertions elsewhere continue passing — add scoped exact-snapshot corpus runs for every checked-in expectation, or declare snapshot ownership in the manifest and have the runner enforce all declared snapshots

- [ ] packages/pdf2md/src/index.d.ts:260 — `WebGpuExecutionSkippedPageDiagnostics` omits the runtime `"exceeds-batch-pixel-limit"` reason — TypeScript consumers cannot accurately type valid diagnostics returned when a routed page exceeds the GPU batch pixel limit — add the runtime literal to the public reason union and cover it with a declaration/output contract test
- [ ] packages/pdf2md/src/index.d.ts:143 — public `Diagnostics.extraction` omits runtime `tables`, `lowConfidenceTables`, `rulingLines`, `rulingGrids`, and `rulingTables`, and `RasterDiagnostics` omits `retention` — TypeScript consumers cannot access tested diagnostic output without casts and declaration drift can continue unnoticed — define the missing structured types and add a compile-time assertion that a real result shape satisfies `ConvertResult`
- [ ] packages/pdf2md/src/schema.mjs:112 — `pageIrJsonSchema.elements.items` accepts any object instead of validating the declared `PageElement` union — invalid or drifted text/table/figure/equation/form/annotation elements pass the exported public schema and give downstream validators false confidence — add discriminated per-element schemas (and keep them synchronized with declarations through schema contract tests)
- [ ] packages/pdf2md/examples/node-basic.mjs:4 — package examples import implementation files through `../src/*` (also browser and worker examples) instead of the required public `pdf-2-llm/*` subpaths — copy-pasted integrations bypass the export map and couple consumers to repository layout rather than testing the published contract — switch public examples to package subpath imports and provide any local import-map/workspace setup needed to keep them runnable from the checkout

- [ ] corpus/baselines/plos-interpretable-ml-reliability-2023/tools/pdfinfo.json:5 — committed tool baselines embed the generating developer's absolute checkout path across 95 JSON files while omitting the external tool versions that determine their output — regenerated artifacts churn across machines, expose local filesystem details, and leave meaningful oracle drift unattributable — serialize repo-relative paths or redact `repoRoot`, and persist each tool's version with its capture
- [ ] corpus/reports/webgpu-benchmark.json:3 — the committed release-evidence report uses one iteration with no warmup and predates the producer's current preprocessing-speedup fields, while `qa:webgpu-comparison:report` specifies three iterations and one warmup — single-sample CPU-fallback ratios such as 3.07x are dominated by startup noise and the artifact cannot evidence the current WebGPU path — send the smoke command to `.temp/qa`, then regenerate this stable report with the representative report command
- [ ] corpus/reports/text-throughput-benchmark.json:2 — committed throughput and memory baselines record only a timestamp, without the package revision, Node version, OS/architecture, or CPU identity of the benchmark host — later comparisons cannot distinguish code regressions from runtime or hardware changes — add a shared execution-environment block to benchmark reports and capture package version, git revision, Node, platform, architecture, and CPU model when generating stable evidence
- [ ] corpus/reports/corpus-inventory.md:38 — the merged feature column appends the detector property `acroForm` beside manifest tags such as `acroform` and `acroform-metadata` — equivalent capabilities appear as separate case-sensitive features, making inventory filtering and counts ambiguous — map detected properties to the canonical manifest feature vocabulary or render declared and detected features in separate columns

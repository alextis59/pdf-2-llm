## Findings

- [x] .github/workflows/ci.yml:34 — CI runs Rust, lint, build, TypeScript tests, and corpus smoke before invoking `npm run check`, which repeats several of the same gates — pull requests spend unnecessary runner time on duplicate work and may hit the 25 minute timeout sooner — either rely on `npm run check` for full CI or split `check` into a non-duplicating remainder step (FIXED: CI now runs the full `npm run check` gate once and uploads QA artifacts afterward, with release notes updated to match — verified by `git diff --check`, `rg -n 'ci:rust:test|ci:lint|ci:build|ci:typescript:test|ci:corpus:smoke|npm run check|CI runs' .github/workflows/ci.yml docs/release-notes.md`, and `npm run lint`)
- [x] packages/pdf2md/src/webgpu-preprocess.mjs:277 — WebGPU preprocessing buffers are destroyed only after successful readback — failed `mapAsync`, command submission, or shader execution paths can leave GPU buffers allocated longer than necessary — wrap runner buffer lifetimes in `try/finally` and destroy all created buffers on every exit path (FIXED: both WebGPU preprocessing runners now destroy all created buffers in `finally`, unmapping readback buffers when needed, with failure-path coverage for rejected `mapAsync` — verified by `node --test packages/pdf2md/test/webgpu-preprocess.test.mjs` and `npm run lint`)
